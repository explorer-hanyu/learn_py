# Python3 元组(Tuple)


## 一、元组概述

Python 的元组与列表类似，不同之处在于**元组的元素不能修改**。

| 对比项 | 元组 | 列表 |
|--------|------|------|
| 符号 | 小括号 `()` | 方括号 `[]` |
| 可变性 | **不可变**（元素不能修改） | 可变（可增删改） |
| 速度 | 更快 | 较慢 |
| 内存占用 | 更小 | 较大 |


## 二、创建元组

创建元组很简单，只需要在括号中添加元素，并使用逗号隔开即可。

```python
tup1 = ('Google', 'Runoob', 1997, 2000)
tup2 = (1, 2, 3, 4, 5)
tup3 = "a", "b", "c", "d"   # 不需要括号也可以
```

### 空元组

```python
tup1 = ()
```

### 单元素元组（特别注意）

元组中只包含一个元素时，**需要在元素后面添加逗号 `,`**，否则括号会被当作运算符使用。

```python
tup1 = (50)      # 不加逗号，类型为整型
type(tup1)       # <class 'int'>

tup1 = (50,)     # 加上逗号，类型为元组
type(tup1)       # <class 'tuple'>
```


## 三、访问元组

元组与字符串类似，下标索引从 **0** 开始，可以使用下标索引来访问元组中的值。

```python
tup1 = ('Google', 'Runoob', 1997, 2000)
tup2 = (1, 2, 3, 4, 5, 6, 7)

print("tup1[0]: ", tup1[0])      # Google
print("tup2[1:5]: ", tup2[1:5])  # (2, 3, 4, 5)
```

### 索引与切片

```python
tup = ('Google', 'Runoob', 'Taobao', 'Wiki', 'Weibo', 'Weixin')
```

| 表达式 | 结果 | 描述 |
|--------|------|------|
| `tup[1]` | `'Runoob'` | 读取第二个元素 |
| `tup[-2]` | `'Weibo'` | 反向读取，读取倒数第二个元素 |
| `tup[1:]` | `('Runoob', 'Taobao', 'Wiki', 'Weibo', 'Weixin')` | 从第二个开始后的所有元素 |
| `tup[1:4]` | `('Runoob', 'Taobao', 'Wiki')` | 从第二个开始到第四个元素（索引为 3） |


## 四、修改元组

元组中的元素值**不允许修改**，但我们可以对元组进行**连接组合**。

```python
tup1 = (12, 34.56)
tup2 = ('abc', 'xyz')

# 以下修改元组元素操作是非法的
# tup1[0] = 100

# 创建一个新的元组
tup3 = tup1 + tup2
print(tup3)   # (12, 34.56, 'abc', 'xyz')
```

### += 操作符

```python
a = (1, 2, 3)
b = (4, 5, 6)
a += b        # a 变成了一个新的元组
print(a)      # (1, 2, 3, 4, 5, 6)
```


## 五、删除元组

元组中的元素值是不允许删除的，但可以使用 `del` 语句来**删除整个元组**。

```python
tup = ('Google', 'Runoob', 1997, 2000)
print(tup)
del tup
print("删除后的元组 tup : ")
print(tup)   # 报错：NameError: name 'tup' is not defined
```


## 六、元组运算符

与字符串一样，元组之间可以使用 `+`、`+=` 和 `*` 号进行运算。运算后会**生成一个新的元组**。

| 表达式 | 结果 | 描述 |
|--------|------|------|
| `len((1, 2, 3))` | `3` | 计算元素个数 |
| `(1, 2, 3) + (4, 5, 6)` | `(1, 2, 3, 4, 5, 6)` | 连接 |
| `('Hi!',) * 4` | `('Hi!', 'Hi!', 'Hi!', 'Hi!')` | 复制 |
| `3 in (1, 2, 3)` | `True` | 元素是否存在 |
| `for x in (1, 2, 3): print(x)` | `1 2 3` | 迭代 |


## 七、元组内置函数

| 函数 | 描述 | 示例 |
|------|------|------|
| `len(tuple)` | 计算元组元素个数 | `len(('Google', 'Runoob', 'Taobao'))` → `3` |
| `max(tuple)` | 返回元组中元素最大值 | `max(('5', '4', '8'))` → `'8'` |
| `min(tuple)` | 返回元组中元素最小值 | `min(('5', '4', '8'))` → `'4'` |
| `tuple(iterable)` | 将可迭代系列转换为元组 | `tuple(['Google', 'Taobao', 'Runoob'])` → `('Google', 'Taobao', 'Runoob')` |


## 八、关于元组的不可变性（重要）

**所谓元组的不可变指的是元组所指向的内存中的内容不可变。** 

```python
tup = ('r', 'u', 'n', 'o', 'o', 'b')
tup[0] = 'g'   # 报错：TypeError: 'tuple' object does not support item assignment
```

### 重新赋值 ≠ 修改

重新赋值会让元组**绑定到新的对象**，而不是修改原来的对象。

```python
tup = ('r', 'u', 'n', 'o', 'o', 'b')
print(id(tup))   # 4440687904（示例地址）

tup = (1, 2, 3)  # 重新赋值，绑定到新对象
print(id(tup))   # 4441088800（地址变了）
```

> **核心理解**：元组不可变是指**不能修改已有元素的值**，但**可以让变量指向一个新的元组对象**。

```


# 📝 综合练习题

## 任务：图书信息管理（元组专项练习）

编写一个名为 `book_manager.py` 的程序，综合运用元组的各种操作完成以下功能。**注意：所有图书信息必须使用元组存储，不可使用列表。**

### 要求

1. **创建元组**
   - 创建一个空元组 `books`（初始为空）
   - 创建 3 个图书元组，每本图书包含：`(书名, 作者, 价格, 库存数量)`
     - `("Python编程", "张三", 59.00, 10)`
     - `("数据结构", "李四", 45.50, 8)`
     - `("算法导论", "王五", 89.00, 5)`
   - 将这 3 个图书元组组合成一个**嵌套元组** `books`（即元组中的每个元素也是一个元组）

2. **访问与查询**
   - 打印所有图书信息
   - 打印第二本书的书名
   - 打印最后一本书的价格（使用负索引）
   - 使用切片打印前两本书

3. **尝试修改（观察报错）**
   - 尝试将第一本书的价格修改为 `69.00`
   - 运行并观察报错信息，用注释记录报错类型

4. **组合与复制**
   - 创建新书元组 `("数据库", "赵六", 55.00, 12)`
   - 使用 `+` 运算符将新书添加到 `books` 中，生成新元组 `new_books`
   - 使用 `*` 运算符将 `("畅销书",)` 重复 3 次，观察结果

5. **元组内置函数**
   - 使用 `len()` 统计当前图书数量
   - 创建一个包含所有书名的元组 `titles`（遍历 `books` 提取）
   - 使用 `max()` 找出价格最高的图书（提示：可以提取价格列表，也可以直接对元组使用 `max()`，观察默认比较规则）
   - 使用 `min()` 找出库存最少的图书
   - 使用 `tuple()` 将列表 `['新书A', '新书B']` 转换为元组并打印

6. **成员检查与迭代**
   - 使用 `in` 运算符检查 `"算法导论"` 是否在书名中
   - 使用 `for` 循环遍历所有图书，打印每本图书的完整信息

7. **不可变性验证**
   - 打印 `books` 的内存地址（使用 `id()`）
   - 将 `books` 重新赋值为一个新的嵌套元组（包含至少 2 本新书）
   - 再次打印内存地址，观察是否变化
   - 用注释解释观察到的现象

```

### 输出示例

```
========== 1. 创建图书元组 ==========
所有图书:
(('Python编程', '张三', 59.0, 10), ('数据结构', '李四', 45.5, 8), ('算法导论', '王五', 89.0, 5))

========== 2. 访问与查询 ==========
第二本书: 数据结构
最后一本书价格: 89.0
前两本书: (('Python编程', '张三', 59.0, 10), ('数据结构', '李四', 45.5, 8))

========== 3. 尝试修改（观察报错） ==========
尝试修改第一本书的价格...
报错信息: TypeError: 'tuple' object does not support item assignment

========== 4. 组合与复制 ==========
添加新书后: (('Python编程', '张三', 59.0, 10), ('数据结构', '李四', 45.5, 8), ('算法导论', '王五', 89.0, 5), ('数据库', '赵六', 55.0, 12))
重复3次: ('畅销书', '畅销书', '畅销书')

========== 5. 内置函数 ==========
图书数量: 4
所有书名: ('Python编程', '数据结构', '算法导论', '数据库')
价格最高的图书: ('算法导论', '王五', 89.0, 5)
库存最少的图书: ('算法导论', '王五', 89.0, 5)
列表转元组: ('新书A', '新书B')

========== 6. 成员检查与迭代 ==========
'算法导论' 在书名中: True
遍历所有图书:
  1. Python编程 - 张三 - 59.0元 - 库存10
  2. 数据结构 - 李四 - 45.5元 - 库存8
  3. 算法导论 - 王五 - 89.0元 - 库存5
  4. 数据库 - 赵六 - 55.0元 - 库存12

========== 7. 不可变性验证 ==========
原 books 内存地址: 4440687904
重新赋值后内存地址: 4441088800
结论: 重新赋值让变量指向了新的元组对象，原元组未被修改。
```

### 自测检查清单

- [ ] 是否使用小括号 `()` 创建了元组？
- [ ] 是否创建了嵌套元组（元组中的元素也是元组）？
- [ ] 是否使用索引和切片访问元组元素？
- [ ] 是否尝试修改元组元素并观察了报错？
- [ ] 是否使用了 `+` 和 `*` 运算符？
- [ ] 是否使用了 `len()`、`max()`、`min()`、`tuple()` 等内置函数？
- [ ] 是否使用了 `in` 和 `for` 循环？
- [ ] 是否使用 `id()` 验证了元组的不可变性？
- [ ] 代码是否无语法错误，能正确运行？