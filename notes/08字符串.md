# Python3 字符串笔记

## 一、字符串概述

字符串是 Python 中最常用的数据类型。可以使用**单引号 `'`** 或**双引号 `"`** 来创建字符串。

创建字符串很简单，只要为变量分配一个值即可：

```python
var1 = 'Hello World!'
var2 = "Runoob"
```


## 二、访问字符串中的值

Python **不支持单字符类型**，单字符在 Python 中也是作为一个字符串使用。

访问子字符串，可以使用**方括号 `[]`** 来截取，语法格式如下：

```python
变量[头下标:尾下标]
```

- 索引值以 **0** 为开始值
- **-1** 为从末尾的开始位置

### 示例

```python
#!/usr/bin/python3
var1 = 'Hello World!'
var2 = "Runoob"

print("var1[0]: ", var1[0])      # 输出: H
print("var2[1:5]: ", var2[1:5])  # 输出: unoo
```


## 三、字符串更新

可以截取字符串的一部分并与其他字段拼接：

```python
#!/usr/bin/python3
var1 = 'Hello World!'
print("已更新字符串 : ", var1[:6] + 'Runoob!')
# 输出: 已更新字符串 : Hello Runoob!
```


## 四、转义字符

在需要使用特殊字符时，Python 用**反斜杠 `\`** 转义字符。

| 转义字符 | 描述 | 实例 |
|----------|------|------|
| `\(在行尾)` | 续行符 | `print("line1 \ ... line2")` |
| `\\` | 反斜杠符号 | `print("\\")` → `\` |
| `\'` | 单引号 | `print('\'')` → `'` |
| `\"` | 双引号 | `print("\"")` → `"` |
| `\a` | 响铃 | 执行后电脑有响声 |
| `\b` | 退格(Backspace) | `print("Hello \b World!")` → `Hello World!` |
| `\000` | 空 | |
| `\n` | 换行 | |
| `\v` | 纵向制表符 | |
| `\t` | 横向制表符 | |
| `\r` | 回车 | 将 `\r` 后面的内容移到字符串开头，并逐一替换 |
| `\f` | 换页 | |
| `\yyy` | 八进制数，y 代表 0~7 的字符 | `\110\145\154\154\157` → `Hello` |
| `\xyy` | 十六进制数，以 `\x` 开头 | `\x48\x65\x6c\x6c\x6f` → `Hello` |
| `\other` | 其它的字符以普通格式输出 | |

### 使用 `\r` 实现百分比进度

```python
import time
for i in range(101):
    bar = '[' + '=' * (i // 2) + ' ' * (50 - i // 2) + ']'
    print(f"\r{bar} {i:3}%", end='', flush=True)
    time.sleep(0.05)
print()
```


## 五、字符串运算符

假设 `a = "Hello"`，`b = "Python"`：

| 操作符 | 描述 | 实例 |
|--------|------|------|
| `+` | 字符串连接 | `a + b` → `HelloPython` |
| `*` | 重复输出字符串 | `a * 2` → `HelloHello` |
| `[]` | 通过索引获取字符串中字符 | `a[1]` → `e` |
| `[ : ]` | 截取字符串中的一部分，**左闭右开** | `a[1:4]` → `ell` |
| `in` | 成员运算符，包含返回 True | `'H' in a` → `True` |
| `not in` | 成员运算符，不包含返回 True | `'M' not in a` → `True` |
| `r/R` | 原始字符串，不转义特殊字符 | `print(r'\n')` → `\n` |
| `%` | 格式字符串 | 见下一节 |


## 六、字符串格式化

Python 支持格式化字符串的输出，使用与 C 中 `sprintf` 函数一样的语法：

```python
#!/usr/bin/python3
print("我叫 %s 今年 %d 岁!" % ('小明', 10))
# 输出: 我叫 小明 今年 10 岁!
```

### 格式化符号

| 符号 | 描述 |
|------|------|
| `%c` | 格式化字符及其ASCII码 |
| `%s` | 格式化字符串 |
| `%d` | 格式化整数 |
| `%u` | 格式化无符号整型 |
| `%o` | 格式化无符号八进制数 |
| `%x` | 格式化无符号十六进制数 |
| `%X` | 格式化无符号十六进制数（大写） |
| `%f` | 格式化浮点数字，可指定小数点后的精度 |
| `%e` | 用科学计数法格式化浮点数 |
| `%E` | 作用同 `%e` |
| `%g` | `%f` 和 `%e` 的简写 |
| `%G` | `%f` 和 `%E` 的简写 |
| `%p` | 用十六进制数格式化变量的地址 |

### 格式化操作辅助指令

| 符号 | 功能 |
|------|------|
| `*` | 定义宽度或者小数点精度 |
| `-` | 用做左对齐 |
| `+` | 在正数前面显示加号( `+` ) |
| 空格 | 在正数前面显示空格 |
| `#` | 在八进制数前面显示 `'0'`，在十六进制前面显示 `'0x'` 或 `'0X'` |
| `0` | 显示的数字前面填充 `'0'` 而不是默认的空格 |
| `%` | `'%%'` 输出一个单一的 `'%'` |
| `(var)` | 映射变量(字典参数) |
| `m.n.` | m 是显示的最小总宽度，n 是小数点后的位数 |


## 七、f-string（Python 3.6+）

f-string 是 Python 3.6 之后版本添加的**字面量格式化字符串**，是新的格式化字符串语法。

### 基本用法

以 `f` 开头，字符串中的表达式用**大括号 `{}`** 包起来，会将变量或表达式计算后的值替换进去：

```python
>>> name = 'Runoob'
>>> f'Hello {name}'          # 替换变量
'Hello Runoob'

>>> f'{1+2}'                 # 使用表达式
'3'

>>> w = {'name': 'Runoob', 'url': 'www.runoob.com'}
>>> f'{w["name"]}: {w["url"]}'
'Runoob: www.runoob.com'
```

### Python 3.8+ 新特性：`=` 符号

在 Python 3.8 中可以使用 `=` 符号来**拼接运算表达式与结果**：

```python
>>> x = 1
>>> print(f'{x+1=}')  # Python 3.8
x+1=2
```


## 八、三引号

Python 三引号（`'''` 或 `"""`）允许一个字符串**跨多行**，字符串中可以包含换行符、制表符以及其他特殊字符。

```python
#!/usr/bin/python3
para_str = """这是一个多行字符串的实例
多行字符串可以使用制表符 TAB ( \t )。
也可以使用换行符 [ \n ]。
"""
print(para_str)
```

三引号让程序员从引号和特殊字符串的转义中解脱出来，保持字符串的**所见即所得**（WYSIWYG）格式。

**典型用例**：当需要一块 HTML 或 SQL 时：

```python
errHTML = '''
<HTML><HEAD><TITLE>
Friends CGI Demo</TITLE></HEAD>
<BODY><H3>ERROR</H3>
<B>%s</B>
<PRE>%s</PRE>
</BODY></HTML>
'''

cursor.execute('''
CREATE TABLE users (
    login VARCHAR(8),
    uid INTEGER,
    prid INTEGER
)
''')
```


## 九、Unicode 字符串

- **Python 2**：普通字符串以 8 位 ASCII 码存储，Unicode 字符串存储为 16 位 unicode 字符串，使用前缀 `u`
- **Python 3**：**所有的字符串都是 Unicode 字符串**


## 十、字符串内建函数（常用）

| 方法 | 描述 |
|------|------|
| `capitalize()` | 将字符串的第一个字符转换为大写 |
| `count(str, beg=0, end=len(string))` | 返回 str 在 string 里面出现的次数 |
| `endswith(suffix, beg=0, end=len(string))` | 检查字符串是否以 suffix 结束 |
| `find(str, beg=0, end=len(string))` | 检测 str 是否包含在字符串中，返回索引值，否则返回 -1 |
| `isalnum()` | 如果字符串至少有一个字符，且所有字符都是字母或数字，返回 True |
| `isalpha()` | 如果字符串至少有一个字符，且所有字符都是字母或中文字，返回 True |
| `isdigit()` | 如果字符串只包含数字则返回 True |
| `islower()` | 如果字符串中至少有一个区分大小写的字符，且都是小写，返回 True |
| `isupper()` | 如果字符串中至少有一个区分大小写的字符，且都是大写，返回 True |
| `join(seq)` | 以指定字符串作为分隔符，将 seq 中所有元素合并为一个新的字符串 |
| `len(string)` | 返回字符串长度 |
| `lower()` | 转换字符串中所有大写字符为小写 |
| `lstrip()` | 截掉字符串左边的空格或指定字符 |
| `replace(old, new, max)` | 将字符串中的 old 替换成 new |
| `rfind()` | 类似于 find()，从右边开始查找 |
| `rstrip()` | 删除字符串末尾的空格或指定字符 |
| `split(str, num)` | 以 str 为分隔符截取字符串 |
| `startswith(substr, beg=0, end=len(string))` | 检查字符串是否以指定子字符串开头 |
| `strip()` | 执行 lstrip() 和 rstrip() |
| `swapcase()` | 将字符串中大写转换为小写，小写转换为大写 |
| `title()` | 返回"标题化"的字符串，所有单词以大写开头 |
| `upper()` | 转换字符串中所有小写字符为大写 |


# 📝 综合练习题

## 任务：个人信息格式化工具

编写一个名为 `string_formatter.py` 的程序，综合运用字符串的各种操作完成以下功能：

### 要求

1. **字符串创建与访问**
   - 创建字符串 `full_name = "Zhang San"`，使用方括号分别输出：
     - 第一个字符
     - 最后一个字符（使用负索引）
     - 姓氏（前 5 个字符）
     - 名字（从索引 6 到末尾）

2. **字符串更新与拼接**
   - 将 `full_name` 更新为 `"Zhang Sanfeng"`（在末尾拼接 `"feng"`）
   - 使用 `+` 运算符将 `"Hello, "` 与更新后的名字拼接，输出完整问候语

3. **转义字符练习**
   - 使用转义字符输出以下格式（**必须使用转义字符实现换行和制表符**）：
     ```
     姓名:   张三
     年龄:   25
     职业:   工程师
     ```
   - 使用原始字符串 `r''` 输出路径：`C:\Users\name\Documents\file.txt`（反斜杠不被转义）
   - 使用 `\r` 实现一个简单的倒计时效果：打印 `5` → `4` → `3` → `2` → `1` → `0`，每次覆盖前一个数字

4. **字符串运算符**
   - 使用 `*` 运算符生成一个由 `"="` 重复 30 次的分隔线
   - 使用 `in` 和 `not in` 判断字符串 `"Python3 字符串练习"` 中是否包含 `"Python"` 和 `"Java"`

5. **字符串格式化（三种方式）**
   - **`%` 格式化**：定义变量 `name="李四"`、`age=28`、`score=92.5`，使用 `%s`、`%d`、`%.1f` 格式化输出：`"李四，28岁，成绩92.5分"`
   - **`str.format()`**：使用 `format()` 方法完成同样的输出
   - **f-string**：使用 f-string 完成同样的输出（要求至少包含一个表达式，如 `age + 5`）

6. **三引号多行字符串**
   - 使用三引号定义一个多行字符串 `info`，内容为：
     ```
     ========== 用户信息 ==========
     姓名：王五
     城市：北京
     签名：好好学习，天天向上！
     ================================
     ```
   - 打印该字符串

7. **字符串方法练习**
   - 给定字符串 `text = "  hello world!  "`
   - 去除首尾空格（使用 `strip()`）
   - 将首字母大写（使用 `capitalize()`）
   - 将所有单词首字母大写（使用 `title()`）
   - 将字符串中的所有字母转为大写
   - 查找 `"world"` 的起始索引（使用 `find()`）
   - 将 `"world"` 替换为 `"Python"`（使用 `replace()`）
   - 使用 `split()` 将字符串按空格分割成列表
   - 使用 `join()` 将列表重新合并为字符串，用 `"-"` 连接

8. **综合挑战**
   - 创建一个函数（或代码块），接收用户输入的姓名和年龄，使用 f-string 输出：`"你好，[姓名]！你输入的年龄是[年龄]岁，5年后你将[年龄+5]岁。"`

### 输出示例

```
========== 1. 字符串访问 ==========
第一个字符: Z
最后一个字符: n
姓氏: Zhang
名字: San

========== 2. 字符串更新 ==========
更新后: Zhang Sanfeng
问候语: Hello, Zhang Sanfeng

========== 3. 转义字符 ==========
姓名:   张三
年龄:   25
职业:   工程师
原始路径: C:\Users\name\Documents\file.txt
倒计时: 0

========== 4. 字符串运算符 ==========
==============================
'Python' 在字符串中: True
'Java' 在字符串中: False

========== 5. 字符串格式化 ==========
% 格式化: 李四，28岁，成绩92.5分
format()  : 李四，28岁，成绩92.5分
f-string  : 李四，28岁，成绩92.5分

========== 6. 三引号多行字符串 ==========
========== 用户信息 ==========
姓名：王五
城市：北京
签名：好好学习，天天向上！
================================

========== 7. 字符串方法 ==========
strip(): hello world!
capitalize(): Hello world!
title(): Hello World!
upper(): HELLO WORLD!
find('world'): 6
replace: hello Python!
split: ['hello', 'world!']
join: hello-world!

========== 8. 综合挑战 ==========
请输入姓名：张三
请输入年龄：25
你好，张三！你输入的年龄是25岁，5年后你将30岁。
```

### 自测检查清单

- [ ] 是否使用了方括号索引和切片访问字符串？
- [ ] 是否使用了 `+` 和 `*` 字符串运算符？
- [ ] 是否使用了 `\n`、`\t`、`\r` 等转义字符？
- [ ] 是否使用了原始字符串 `r''`？
- [ ] 是否使用了 `%`、`format()`、f-string 三种格式化方式？
- [ ] 是否使用了三引号创建多行字符串？
- [ ] 是否使用了 `strip()`、`capitalize()`、`title()`、`upper()`、`find()`、`replace()`、`split()`、`join()` 等方法？
- [ ] 是否使用了 `in` 和 `not in` 成员运算符？
- [ ] 代码是否无语法错误，能正确运行？

完成练习后，如果遇到问题随时来问我 😊